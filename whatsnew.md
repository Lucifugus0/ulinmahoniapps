# What's New — Ulin Mahoni

## Backend

### New Modules

| Date | Page/Screen | Description |
|------|-------------|-------------|
| 2026-05-04 | Modify Booking (`/bookings/modify-booking`) | New admin tool for editing scheduled `check_in` / `check_out` and (BRI Manual only) `paid_at` on paid bookings after they've been confirmed but typically before checkout. **List filter** is identical to Confirmed Bookings: paid + active + not yet checked out + `latestPerOrder` + property scope for site-role admins + same search/date-range. **Action button** opens a single shared modal that fetches the booking's transaction snapshot + modification history via `GET /bookings/modify-booking/{order_id}/details`. **Editable fields**: `t_transactions.check_in` / `check_out` (datetime-local inputs) plus `t_transactions.paid_at` — the paid_at field is enabled only when `transaction_type IN ('bri_manual','BRI Manual')` and disabled+labeled-as-locked otherwise (DOKU webhook controls that timestamp for QRIS / VA / CC channels). **Audit trail** uses Option A: each save clones the active `t_booking` row, sets the parent's `status=0`, points the clone's `previous_booking_id` at the parent, and stamps the clone with `modified_at` / `modified_by` / `modification_type` (`date` / `payment_date` / `date+payment_date`) / `modification_notes`. `latestPerOrder` continues to pick up just the active clone so list pages everywhere stay coherent. **History panel** in the modal queries all `t_booking` rows for the same `order_id` where `modified_at IS NOT NULL`, newest first, and renders one card per modification with timestamp, admin name, modification type, and optional note. **Schema additions** — migration `2026_05_04_120000_add_modification_audit_to_t_booking` adds 4 columns to `t_booking` (`modified_at`, `modified_by`, `modification_type`, `modification_notes`), all nullable, applied to production via direct ALTER. **Sidebar entry** added under Bookings group, immediately below Change Booking, gated by the same `view_change_room` permission. **i18n** — 18 new `modify_booking_*` + `sidebar_modify_booking` keys added to `lang/{en,id}/ui.php`. **Files**: `Backend/app/Http/Controllers/Bookings/ModifyBooking/ModifyBookingController.php` (list + filter + show JSON + modify POST), `Backend/app/Models/Booking.php` (extended `$fillable` + new `modifiedByUser()` relation), `Backend/routes/web.php` (4 new routes), `Backend/resources/views/components/app/sidebar.blade.php` (sidebar item), `Backend/resources/views/pages/bookings/modify/index.blade.php` + 2 partials. |
| 2026-04-30 | Room Availability — Booking Reference column (`/rooms/room-availability`) | Replaced the modal-style "Related Bookings" column (button → modal listing) with an inline **Booking Reference** column that renders one badge per paid booking, stacked vertically. Two badge colors: **blue** = guest currently in the room (`check_in_at NOT NULL AND check_out_at NULL`), **green** = future paid booking (`check_in_at NULL AND scheduled check_in >= today`). Each badge shows the customer's first + last name on line 1 (with fallback to `t_transactions.user_name` / `t_booking.user_name`) and the scheduled stay period (`d M Y → d M Y`) on line 2. Eager-load tightened to require `transaction_status = 'paid' AND renewal_status = 0` (excludes superseded parents) so renewed parent rows no longer show up as separate badges; bucket-split into current/future happens in the blade. Old "Currently Occupied By" column removed (its content is now the blue badge). New i18n key `booking_reference` ("Booking Reference" / "Referensi Booking") added to `Backend/lang/{en,id}/ui.php`; existing `no_bookings` key reused for the empty state. Pure controller + Blade + i18n edit; no migration / model / API change. |
| 2026-04-30 | Dashboard — Pending check-outs widget (`/dashboard`) | Bumped the upcoming check-out widget's hard limit from 4 to 5 rows (`DashboardController::index` query `->limit(4)` → `->limit(5)`) so admins can see one more upcoming row at a glance without leaving the dashboard. Replaced the previous footer line ("Showing X of Y check-outs in the next 3 days") with a static helper note: **"Showing top 5 data. Click VIEW MORE for all data."** Indonesian: "Menampilkan 5 data teratas. Klik VIEW MORE untuk semua data." New i18n key `checkout_widget_top_note` added to `Backend/lang/{en,id}/ui.php`. **Consistency pass with Today's Check-out page (same day):** the widget previously showed only `today … today+3` and ignored renewal-supersession filters, so it could disagree with the Check-out page in two ways — (a) renewed parent bookings (parent transaction's `renewal_status = 1`) appeared on the widget but not the page, and (b) overdue rows shown on the page were absent from the widget. Three filter changes applied: (1) added `->latestPerOrder()` scope to collapse renewal chains, (2) added `where('renewal_status', 0)` to the transaction `whereHas` so superseded parents are excluded, (3) dropped the lower date bound — window is now `check_out <= today+3` so overdue + today + next 3 days are all included. **"View All" link bug fixed**: the link in the widget header was routing to `checkin.index` with 3-day date params (wrong page) — now routes to `checkout.index` (no params) which is the matching list page. Header text `checkout_reminder` updated to "Pending check-outs (overdue + next 3 days)" / "Check-out tertunda (lewat tenggat + 3 hari ke depan)" to reflect the new semantics. Pure controller + Blade + i18n edit; no migration / model / API change. |
| 2026-04-29 | Room Types — Sort Priority (`/properties/rooms/room-name-types`) | Added admin-controlled ordering for the master room types (Standar/Superior/Deluxe/Suite/etc.). New **Sort Priority** column in the table renders the priority number alongside up/down chevron buttons; clicking either swaps `m_room_name_types.sort_priority` with the immediate neighbour across the entire table (not just the current paginated page slice), so an "up" click on the first row of page 2 swaps with the last row of page 1. **Backend changes:** migration `2026_04_29_100001_add_sort_priority_to_m_room_name_types.php` adds `sort_priority UNSIGNED INT DEFAULT 0` and backfills existing rows with sequential 1..N based on `idrec` order; `RoomNameType::$fillable` extended; `ManajementRoomsController::indexRoomNameType()` now `orderBy('sort_priority', 'asc')->orderBy('name', 'asc')`; new `reorderRoomNameType(Request)` endpoint with `direction=up\|down` validation, neighbour lookup, and atomic swap inside `DB::transaction`; new route `POST /properties/rooms/room-name-types/reorder`; partial `room_name_type_table.blade.php` gains the Sort Priority column (chevron up + number + chevron down) and updated colspan from 5 to 6; `index.blade.php` adds `reorderRoomNameType(id, direction)` JS that POSTs and reloads on success or shows a SweetAlert toast on neighbour-not-found / errors; new i18n keys `room_name_type_col_sort_priority`, `room_name_type_move_up`, `room_name_type_move_down` in `lang/{en,id}/ui.php`. **Frontend Web (`/houses/{id}` "Kamar Tersedia"):** the existing `groupBy('name')` in `pages/house/show.blade.php` now also `sortBy(...)` the outer group order using `m_room_name_types.sort_priority` (queried inline); unknown names fall to the end. **Mobile App (Property detail "Ruangan Tersedia" filter dropdown):** Frontend API `Api/RoomController::byPropertyId()` now adds `type_sort_priority` to each room (sourced from `m_room_name_types.name → sort_priority` map); Mobile `RoomModel` parses `typeSortPriority`; `rooms.dart` filter now builds a `name → priority` map from the loaded rooms and sorts the dropdown by priority asc instead of alphabetical. Pure additive API change — old apps ignore the new field. |
| 2026-04-17 | Invoice Numbering System (`InvoiceNumberService`, `t_transactions.invoice_number`, `m_invoice_sequences`) | Replaced on-the-fly invoice number generation with a persisted, atomic, per-invoice-code shared sequence. New format `{seq:4}/{property_initial}/{invoice_code}-INV/{roman_month}/{year}` (e.g. `0001/K1/KGA-INV/III/2026`). Sequence is shared across booking (`t_transactions`) and parking (`t_parking_fee_transaction`) for properties with the same `invoice_code`, resets every January 1, starts from 2026-03-06 (older transactions stay null). Counter table `m_invoice_sequences` (composite unique key on invoice_code+year) is incremented inside `DB::transaction` + `lockForUpdate()` to prevent collisions. Numbers are assigned the moment a transaction flips to `paid` (PaymentController.approve, ParkingPaymentController.approve+store) and never recomputed — refunded/completed transactions keep their number (rule #8). Display layer (`NewReservController::getInvoice()`, `PaymentReportController`, `PaymentReportExport`, `invoice.blade.php`) reads the column instead of regenerating. New artisan command `php artisan invoices:backfill [--dry-run]` walks paid bookings + parking ≥ 2026-03-06 chronologically and assigns numbers per shared counter; deployed to staging (170 entries) + production (170 entries: 156 bookings incl. refunded + 14 parking incl. completed). Two new migrations: `2026_04_17_100001_add_invoice_number_to_t_transactions` (unique varchar 100), `2026_04_17_100002_create_m_invoice_sequences_table`. Old PRK-prefixed parking invoices (3 historical post-cutoff entries) renumbered into new shared format; pre-cutoff PRK numbers untouched. |
| 2026-04-14 | Add/Edit Property modal (`/m-properties`) | Step 1 layout cleanup pass 2: resized the Name / Invoice Code / Initial row from 6/3/3 to 4/4/4 so both short inputs have enough horizontal space for 10 characters and their multi-word labels no longer wrap. Removed the `text-center` class from Invoice Code and Initial so long values align left like a normal text input. Converted Property Type from a 2-column radio-button grid to a `<select>` dropdown. Property Type and Tenant Gender are now placed side-by-side in a single `md:grid-cols-2` row (gender column shows a muted "only for Kos" hint when the type isn't Kos instead of disappearing and shifting the layout). |
| 2026-04-14 | Add/Edit Property modal (`/m-properties`) | Added new **Property Invoice Code** field to the Basic Information step, positioned between Property Name and Property Initial. Both the Invoice Code and Initial inputs accept up to 10 characters, auto-uppercase on input, and the row is resplit as 6/3/3 so each of the two short inputs is wide enough to display its full 10-character value. Also renamed the existing "Initial" label to "Property Initial" for clarity. New nullable column `invoice_code` (varchar 10) added to `m_properties` via migration `2026_04_14_100001_add_invoice_code_to_m_properties.php`; `Property` model `$fillable` and `ManajementPropertiesController` `store`/`update` validation + persistence updated. Invoice Code is optional (nullable) — Initial remains required. |
| 2026-04-08 | All Pages | Web push notification support via Firebase Cloud Messaging — browser sessions now register FCM tokens and receive push notifications for all events (chat, bookings, etc.). |
| 2026-03-31 | Property & Room Description | Rich text editor (Quill.js) for property and room descriptions — supports bold, italic, text color, bullet/numbered lists, and links. Replaces plain textarea in multi-language description component. |
| 2026-03-31 | Refund Management (`/refund`) | Enhanced refund page — shows Source column (User Request/Admin badge), refund breakdown (room/deposit/parking), bank account details for QRIS/VA user-initiated refunds, admin notes field in confirmation modal. |
| 2026-03-31 | Booking Cancellation (PaymentController) | Admin cancel now uses RefundCalculationService for automatic refund breakdown calculation with tiered percentages. |
| 2026-03-31 | RefundCalculationService | New service for calculating refund amounts — tiered room+parking refund (>10d:75%, 9-7d:50%, 6-3d:25%, <3d:0%), deposit always 100%, service/admin fees not refunded. |
| 2026-03-31 | Database: `t_refund` (migration) | Added columns: `requested_by`, `refund_type`, `refund_bank_name`, `refund_account_no`, `refund_account_holder`, `room_refund`, `deposit_refund`, `other_refund`, `admin_notes`, `processed_by`, `processed_at`. |
| 2026-03-30 | Property & Room CRUD | Multi-language description support (ID/EN/ZH) — tabbed textarea with auto-translate via Google Translate. Descriptions stored as XML-tagged string in existing column. |
| 2026-03-30 | Translation API (`/api/translate`) | Backend endpoint for auto-translating description text between Indonesian, English, and Simplified Chinese using `stichoza/google-translate-php`. |
| 2026-03-27 | Content Management (`/settings/content-management`) | Tagline & hero video management — create/edit/delete taglines (displayed randomly on home pages), upload/activate/delete hero background videos (MP4, max 100MB, 21:9 ratio). Two-tab Alpine.js interface with AJAX CRUD. |
| 2026-03-27 | Content API (`/api/v1/content/*`) | Public endpoints for random tagline and active hero video URL — used by Frontend web and Mobile app. |
| 2026-03-27 | Database: `m_taglines`, `m_hero_videos` | New master tables for dynamic tagline and hero video content management. |
| 2026-03-26 | Ticket Inbox (`/tickets`) | Customer service ticketing system — two-panel layout with ticket list, chat detail, status filters, search. Staff can send text/image messages, close/reopen tickets. Auto-transitions to "in progress" on first staff reply. |
| 2026-03-26 | Broadcast Management (`/tickets/broadcasts`) | One-way announcement system — create, list, and view broadcasts. Supports Front Desk (property-scoped) and HQ audiences. Sends FCM push to all target users. |
| 2026-03-26 | Sidebar: Customer Service | New collapsible sidebar group with Tickets + Broadcasts sub-items, unread badge counter (30s polling). |
| 2026-03-26 | Firebase Notification Service | FCM v1 push notification service for Backend. Sends notifications on chat messages, ticket events, and broadcasts. |
| 2026-03-26 | Ticket Database (8 tables) | `t_tickets`, `t_ticket_messages`, `t_ticket_attachments`, `t_ticket_reads`, `t_ticket_categories`, `t_ticket_sequences`, `t_ticket_broadcasts`, `t_ticket_broadcast_reads`. |
| 2026-03-26 | Ticket Categories Seeder | 12 predefined categories: booking (payment, parking, extend, others), complaint (room, noise, electricity, water, wifi, tv, others), suggestion (feedback). |
| 2026-03-26 | Customer Service Role | New "Customer Service" role seeded in `m_roles` for HQ CS ticket routing. |

### Bug Fixes

| Date | Page/Screen | Description |
|------|-------------|-------------|
| 2026-05-04 | t_transactions (production data repair) — backdate audit + chain repair for post-2026-03-06 entries | Audited every paid/expired/pending transaction created after 2026-03-06 where `DATE(check_in) < DATE(created_at)`, identified 9 violating records across 7 users plus Muhammad Brilian's chain (which surfaced first). Applied per-chain repair rules: **(rule A) initial bookings with deposit + paid via DOKU AFTER created_at → fix** (clear admin date-entry error); **(rule B) renewals (`is_renewal=1`, deposit=0) → keep as-is** (legitimate renewals; the backdate is attributed to the prior check-in/check-out date-clamp bug fixed earlier); **(rule C) initial bookings where `paid_at < created_at` → keep as-is** (genuine retroactive backdate entries — admin recording history rather than creating future bookings). **Records repaired**: Muhammad Brilian's UMH-260321513K1 + UMH-260421746K1 (+1d each, OCD 20→21), plus parent UMH-260221044K1's `renewal_status=1→0` (its renewal attempt UMH-260320091K1 expired and the next paid transaction was a fresh contract — never actually renewed; the cron skip-guard had been incorrectly preserving its renewed state because it didn't detect the chain break); Tandjung Zamzami's UMH-260403186K2 + cascade UMH-260502179K2 (+1d each, OCD 2→3); lutfi hafiludin's UMH-260311725K2 (+1d, daily, no cascade); Dea Aprilia's UMH-260406754K1 (+1d, OCD 5→6, the fresh contract after she was checked out and re-checked in at #310). Each shifted record had its `check_in`, `check_out`, and `original_checkin_day` updated; `t_booking.check_in_at`/`check_out_at` left untouched (those are physical timestamps, independent of scheduled dates). **Records left intact**: Indri Lestari, Risnu Ardian, Fathir Naufal (rule B); Dea Aprilia's earlier renewal chain (rule B); Riyan Iman (rule C). **Skip-guard caveat noted** for cron `expire_transactions.sql`: the current "newer paid renewal exists" check is too lenient when a chain BREAKS (a fresh deposit-paying booking lands between parent and the next renewal) — it falsely classifies a downstream renewal of a NEW contract as protective for an UNRELATED parent. Followup needed: tighten the skip-guard so a fresh `is_renewal=0` booking between parent and the candidate "newer renewal" invalidates the protection. |
| 2026-05-04 | Bookings pages — unified column layout across Confirmed Bookings, Checked-In, Today's Check-Out (`/bookings/newReserv`, `/bookings/checkin`, `/bookings/checkout`) | Extended the All Bookings 5-column layout (Booking ID + Booking Period + Name + Property/Room + Status, plus optional Action) to the three other tabs that previously had their own ad-hoc columns. **Each page's table partial rewritten** to match the All Bookings row template: `Booking ID` cell with order_id, transaction `created_at`, and the at-a-glance Deposit / Renewed / Checked Out badges; merged `Booking Period` cell with the 3-row stack (scheduled in/out, actual check-in `by` admin, actual check-out / Extended at); `Name` cell with full email/phone fallback chain (transaction → booking → user → '-'); `Property/Room`; page-specific Status. **Page-specific status preserved**: Confirmed Bookings keeps the `Waiting For Check-In` cyan + the standard lifecycle states with the Renewed override; Checked-In renders Checked-In/Checked-Out/Renewed with dark-mode variants; Today's Check-Out keeps its Overdue (red) / Due Today (yellow) badge AND tints the scheduled `check_out` date red in the Booking Period cell when overdue. **Action columns kept at the rightmost slot**: Confirmed Bookings retains its check-in modal / print-registration-form button / 600-line inline check-in modal verbatim; Today's Check-Out retains its checkout modal trigger; Checked-In keeps the optional `$showActions ?? true` slot. **Eager-loads added** to `CheckInController::index()` + `::filter()`, `CheckOutController::index()` + `::filter()`, and `NewReservController::filterBookings()` for `checkedInByUser` + `checkedOutByUser` so the by-admin attribution doesn't N+1. The shared `allbookings_dates_col` / `allbookings_col_*` / `allbookings_badge_*` translation keys are reused across all four partials so the column headers and badges stay perfectly in sync. Pure controller + Blade edit; no migration / model / API change. Pending and Completed already share the All Bookings partial so they're automatically aligned. |
| 2026-05-04 | All Bookings — column reorder + rename + dark-mode badge fixes (`/bookings/bookings`) | Three follow-up tweaks. **(1) Column rename**: the merged "Check-in / Check-out" header (which had become a confusing dual-clickable label after the prior columns merge) renamed to "Booking Period" / "Periode Pemesanan" via the existing `allbookings_dates_col` key. The dual-clickable sort sub-buttons (one for `check_in`, one for `check_out`) collapsed into a single sortable header that sorts by `t_transactions.check_in` only — sort-by-checkout was rarely used and the merged cell still surfaces both timestamps for visual scanning. **(2) Column swap**: Booking ID is now the leftmost data column, Booking Period sits in the second slot. Matches the new "Booking ID descending" default sort and feels more natural since admins typically read order_id first when triaging a row. The `<th>` order in `<thead>` and the `<td>` order in `<tbody>` were swapped together. **(3) Dark-mode badge contrast**: `Checked-In` (green), `Checked-Out` (red), and `Renewed` (purple) status pills now include explicit dark-mode variants (`dark:bg-{color}-900/40 dark:text-{color}-300`) so the high-traffic states render with proper contrast on the dark theme. Without them, `bg-purple-100` was rendering muddy (visible in the screenshot the user flagged). The less-frequent lifecycle states (Waiting For Payment, Confirming, Waiting Check-In, Canceled, Expired, Rejected, Unknown) keep the light-mode-only classes — they still render readably on dark backgrounds, just with less punch. Pure Blade + i18n edit. |
| 2026-05-04 | All Bookings — Status column: Renewed pseudo-status + Checked-Out red color (`/bookings/bookings`) | Two follow-ons after the status-column simplification. **(1) Renewed pseudo-status.** The `Booking::getStatusAttribute()` accessor returns `Checked-Out` whenever both `check_in_at` and `check_out_at` are set on a paid booking — but for renewal-closed parents (like UMH-260218535K1), `check_out_at` reflects the renewal close-out, not a real departure. So a renewed parent was rendering as Checked-Out, which is misleading (matches the same complaint that drove the "Renewed at:" → "Extended at:" rename earlier today). Added a partial-level override: when `$booking->status === 'Checked-Out' && transaction.renewal_status == 1`, swap the displayed status to a new `Renewed` pseudo-status (purple — `bg-purple-100 text-purple-800`). The accessor and DB are untouched; only the displayed label/color changes. Aligns with the Renewed badge already shown under Booking ID. **(2) Checked-Out badge color.** Switched from indigo (`bg-indigo-100 text-indigo-800`) to red (`bg-red-100 text-red-800`) per design — matches the red Checked Out badge under Booking ID. Genuine admin-driven check-outs now render in red, renewed parents render in purple, and the two are clearly distinguishable. Pure Blade edit. |
| 2026-05-04 | All Bookings — Status column simplified to coarse label only (`/bookings/bookings`) | The Status column previously rendered "Check-in at: YYYY-MM-DD HH:mm" or "Check-out at: ..." pills with the actual timestamp embedded in the badge — duplicating information that now lives in the merged Dates column (rows 2 & 3) and the new Deposit/Renewed/Checked Out badges under Booking ID. Stripped the special Checked-In / Checked-Out branches so the Status column always renders just the coarse lifecycle label (Pending Payment / Confirming / Waiting Check-In / Checked-In / Checked-Out / Canceled / Expired / Rejected / Unknown) using the existing `$statusClasses` color map. The `$statusLabels` array unchanged — same wording, just no embedded timestamp. The full payment-info sub-block (Paid Method / Payment Confirmed By / Payment date+time) under the badge is unaffected. Pure Blade edit. |
| 2026-05-04 | All Bookings — at-a-glance badges under Booking ID (`/bookings/bookings`) | Added a third row to the Booking ID cell with up to 3 small pill badges so admins can see a booking's key state without reading the Status / Check-out columns. **Deposit (green)** when `t_transactions.deposit_fee > 0` — flags initial bookings that carried a deposit. **Renewed (purple)** when `t_transactions.renewal_status = 1` — flags parents superseded by a later renewal. **Checked Out (red)** when `t_booking.check_out_at IS NOT NULL AND renewal_status != 1` — distinguishes a genuine admin-driven physical checkout from a renewal close-out (which also writes `check_out_at` on the parent but isn't a real departure). The badge row is omitted entirely when none of the conditions apply, so unbooked / pending / waiting-check-in rows stay clean. New i18n keys `allbookings_badge_deposit` / `_renewed` / `_checked_out` added to `lang/en/ui.php` (Deposit / Renewed / Checked Out) and `lang/id/ui.php` (Deposit / Diperpanjang / Sudah Check-out). Pure Blade + i18n edit; no controller / model / migration / API change. |
| 2026-05-04 | All Bookings — EN label "Renewed at:" → "Extended at:" (`/bookings/bookings`) | The Check-out column's third line uses the parent's `transaction.renewal_status` to switch the label between "Check-out at:" (genuine departure) and "Renewed at:" (closed by next renewal). The Indonesian translation "Diperpanjang pada:" reads naturally as "extended at" — clearly the parent's perspective — but the English "Renewed at:" was being misread as "this booking is itself a renewal" (e.g. UMH-260321513K1 — `is_renewal=0`, `renewal_status=1`, has a deposit, but the EN label made admins think it was a renewal). Renamed `allbookings_renewed_at` in `Backend/lang/en/ui.php` from "Renewed at:" to "Extended at:" to match the Indonesian semantic. ID file untouched. Pure i18n edit; no controller / model / migration / API change. |
| 2026-05-04 | All Bookings — search box now matches email + phone (`/bookings/bookings`) | Search filter only ran against `order_id` + linked user's `username` / `first_name` / `last_name`, so admins typing a guest email got an empty list even when the booking clearly existed (e.g., `Mhmdbrilian@gmail.com` returned 0 hits despite 6 paid transactions on that email). Extended `AllBookingController::index()` and `::filter()` so the search clause now also matches: `users.email`, `users.phone_number`, plus the denormalized `t_transactions.user_name` / `user_email` / `user_phone_number` (needed because legacy bookings + guest-checkout flows can have NULL `user_id` while still holding contact info on the transaction row). Pure controller edit — no migration / model / view / API change. Other booking pages (Pending, Confirmed, Check-In, Check-Out, Completed) still match only order_id + user name; left unchanged for now since the user only flagged All Bookings, but they have the same shape if it crops up. |
| 2026-05-04 | All Bookings — Check-in / Check-out columns merged into single Dates column (`/bookings/bookings`) | The two leftmost columns (Check-In + Check-Out) carried four loosely-related lines of data each (scheduled date, scheduled time, actual datetime, actual datetime+by-admin), forcing admins to scan across two columns to reconstruct one booking's timeline. Merged into a single column with a clearer 3-line vertical layout: **Row 1** — scheduled `check_in` `|` scheduled `check_out` (date + time, side-by-side, separated by a vertical bar). **Row 2** (only when `check_in_at` is set) — green "Check-in at: YYYY-MM-DD HH:mm by {admin}" using the `checkedInByUser` relation with the same first+last → username → '-' fallback used for customer names elsewhere. **Row 3** (only when `check_out_at` is set) — indigo "Check-out at: YYYY-MM-DD HH:mm by {admin}" using `checkedOutByUser`, OR amber "Renewed at: YYYY-MM-DD HH:mm" with NO `by` attribution when `transaction.renewal_status = 1` (renewals are guest-driven via the app/web — `checked_out_by` is NULL by design, so showing "by —" would be misleading). **Header**: kept both halves individually sortable — clicking "Check-in" or "Check-out" sorts by the respective `t_transactions` column, with the active-sort arrow rendered next to the clicked half. **Backend**: `AllBookingController::index()` and `::filter()` now eager-load `checkedInByUser` + `checkedOutByUser` (FKs `t_booking.checked_in_by` / `checked_out_by` → `users.id`) so the by-admin attribution doesn't N+1. **Files**: `AllBookingController.php` (eager-load update, both branches), `allbookings_table.blade.php` (single merged `<th>` + single merged `<td>` replacing the prior two each, empty-state colspan 6→5), `lang/{en,id}/ui.php` (`allbookings_by` = "by"/"oleh", `allbookings_dates_col` = "Dates"/"Tanggal" reserved for future header use). No migration / model / API change. |
| 2026-05-04 | Booking Payment — clickable Booking ID opens read-only Payment Details modal (`/payment/pay`) | The Booking ID cell on the payment list was a static label — admins had to scroll across the whole row to read out customer name, dates, amount, payment method, etc. Made it a clickable button that opens a per-row modal with the full payment context, formatted as labelled key/value pairs across six sections: **Order** (order_id, invoice number, transaction date, transaction code), **Customer** (name, email, phone with the same `users → t_transactions → '-'` fallback chain used elsewhere), **Property & Room** (name + room number), **Booking Period** (check-in, check-out, booking type, duration in days/months — falls back to `check_in→check_out` diff for legacy rows missing `booking_days`/`booking_months`), **Price Breakdown** (room price, deposit, parking, admin fee, service fee, tax, voucher code + discount, grand total — optional zero-value rows are hidden so admins aren't forced to scan past empty Voucher/Tax/Parking lines for typical bookings), and **Payment** (method, transaction status, payment status, paid_at, verified_by/at, expired_at). Notes appear below in their own block when present. **Method label** uses the same `transaction_type → payment_bank` resolution + label map applied to All Bookings (`bri_manual`/`BRI Manual` → "BRI Manual", `qris`/`QRIS` → "QRIS", `mandiri`/`bri`/`bsi`/`cimb`/`btn`/`danamon` → "{Bank} VA", etc.). **Verifier fallback** uses the `DOKU` rule: when `verified_by` is null but transaction is paid via a DOKU channel (qris/credit_card/VA/bank-name aliases), shows `DOKU` instead of `—`. **Modal mechanics** mirror the existing `rejectModal-*`, `cancelModal-*`, `editPaymentDateModal-*` patterns on this page — per-row markup keyed by `paymentDetailsModal-{idrec}`, vanilla JS show/hide via `.hidden` toggle, Esc-to-close handler, scroll-lock on `body` while open, click-outside-to-close on the backdrop. Read-only — no actions/forms, just display. **Files**: `Backend/resources/views/pages/payment/pay/partials/pay_table.blade.php` (clickable button + JS handlers in the existing `<script>` block + 240-line modal `@foreach` after the existing modal blocks), `Backend/lang/{en,id}/ui.php` (24 new `payment_details_*` keys for the section headers, sub-labels, and duration pluralization). No controller/migration/model/API change. |
| 2026-05-04 | All Bookings — payment method shown for DOKU-paid + Expired bookings (`/bookings/bookings`) | The Status-column sub-block under Checked-In / Checked-Out badges read `t_transactions.payment_bank` for the **Metode Pembayaran** line and `t_payment.verified_by` for **Pembayaran dikonfirmasi oleh** — both fields are only populated by the admin-confirmed BRI Manual flow, so every DOKU-paid booking (QRIS / VA / Credit Card) showed `—` / `—` even though the payment had cleared. **Three changes** applied to `allbookings_table.blade.php`. **(1) Method resolution.** Switched primary source to `t_transactions.transaction_type` (universal — set on every transaction including DOKU channels) with `payment_bank` as the legacy fallback. Channel values are inconsistent across history (`bri_manual` / `BRI Manual` / `qris` / `QRIS` / `credit_card` / `mandiri` / `bri` / `bsi` / `cimb` / `btn` / `danamon` / `Transfer VA` / `Credit Card`) so added a small `$paymentLabels` map that normalizes them to display labels (`BRI Manual`, `QRIS`, `Credit Card`, `Mandiri VA`, `BRI VA`, `BSI VA`, `CIMB VA`, `BTN VA`, `Danamon VA`, `Transfer VA`); unmapped values fall through to uppercase. **(2) Confirmer fallback.** When `verified_by` is null but the transaction was paid via a DOKU channel (qris / credit_card / VA / bank-name aliases), the **Pembayaran dikonfirmasi oleh** line now shows `DOKU` instead of `—`. Admin-confirmed manual payments continue to show the admin's first+last name. Mirrors the convention already used on `pay_table.blade.php`. **(3) Expired bookings.** Added a new sub-block under the Expired badge that renders just the chosen payment method (no confirmer, no paid-at — those are meaningless for an unpaid booking). Surfaces the channel the guest was attempting so admins can still see what was tried. Pure Blade edit; no controller / model / migration / API change. |
| 2026-05-04 | All Bookings — default sort changed to Booking ID descending (`/bookings/bookings`) | Default sort was `check-in descending` which buried newly created bookings under everything that was renewed/back-dated to an earlier check-in. Switched the default to **Booking ID descending** so the freshest activity surfaces at the top of the table — order_id format (`UMH-{ymd}{rand}{property}`) sorts naturally by date, so the newest order ID is the newest booking. **Three files touched**: `AllBookingController::index()` and `::filter()` default `$sortBy` flipped from `'checkin'` → `'orderid'` (and `orderid` is now the `default` switch branch); `allbookings_table.blade.php` `$currentSort` / `$currentDir` defaults changed so the sort-icon arrow renders correctly on initial load; `index.blade.php` JS `window._bookingSortBy/Dir` initial values + the in-flight fallbacks at `params.append(...)` updated to match. Admins can still click any column header to override; this change only affects the *initial* render when no `sort_by` is in the URL. Pure controller + Blade edit; no migration / model / API change. |
| 2026-05-04 | Cron SQL `expire_transactions.sql` — renewal rollback skip-guard (`/home/umadminpanel/sql/expire_transactions.sql`, runs every minute via `~/scripts/live_expire_transactions.sh`) | The minute-cron that flips pending transactions to `expired` was the **third** path that rolls back a renewal's parent state (alongside Laravel's `Jobs/ExpireBooking` and `BookingController::cancelBooking`), but it was the only one missing the `hasNewerSuccessfulRenewal` guard added on 2026-04-29. When two renewal attempts overlap in the same window — e.g. user starts a BRI Manual renewal, abandons it, then completes a QRIS renewal in the next minute — the abandoned attempt's later expiry would clobber the parent's `renewal_status` and `check_out_at` that the successful renewal had already set. **Surfaced via**: Steven / KOST 3 #205 (`UMH-260330356K3`) — abandoned BRI Manual `UMH-260430056K3` created 2026-04-30 15:40:56, successful QRIS `UMH-260430805K3` created 15:41:35 and paid 15:42:55. At 16:10:56 the cron expired `UMH-260430056K3` and the SQL's unconditional `WHEN t.is_renewal = 1 THEN 0` / `THEN NULL` reset parent `UMH-260330356K3`'s `renewal_status` to 0 and `t_booking.check_out_at` to NULL — making it reappear in All Bookings as Checked-In and on the Check-Out Today page. **Fix**: added a `newer_renewal_check` derived table to the multi-table UPDATE that counts paid/confirmed/completed `is_renewal = 1` rows newer than the parent for the same room+user, and gated both `t_old.renewal_status` and `b.check_out_at` resets on `COALESCE(newer_renewal_check.newer_paid_renewals, 0) = 0`. Mirrors the Laravel guard exactly. **Backfill**: production `UMH-260330356K3` → `t_transactions.renewal_status = 1`, `t_booking.check_out_at = '2026-04-30 15:41:35'` (the successful renewal's `created_at`). DB-wide scan found only this one affected record. SQL file is operational tooling outside the git repo; deployed directly to `/home/umadminpanel/sql/` with backup at `expire_transactions.sql.bak.20260504_093512`. |
| 2026-04-30 | Cron `bookings:reset-expired-rooms` — stop auto-flipping `rental_status` (`Backend/app/Console/Commands/ResetExpiredBookingRooms.php`) | The 2-minute scheduled command (`Backend/app/Console/Kernel.php` — `everyTwoMinutes`) was deactivating expired t_booking rows AND resetting `m_rooms.rental_status` to 0 for rooms whose only "non-expired" sibling guard returned false. This violates the standing rental_status rule: "Physical occupancy is owned exclusively by admin check-in / check-out actions; booking lifecycle events MUST NOT auto-flip `m_rooms.rental_status`." **Surfaced via** KOST 1 #219 (room_id 31) — guest Ramdhan Shahaby Abshor checked in via `UMH-260429512K1` at 2026-04-29 20:19:06; an earlier failed/expired attempt by the same guest on the same room (`UMH-260429986K1`) hit the ExpireBooking job at 20:24:01, the 2-min cron tick fired at the same second and reset the room flag to 0 even though the paid check-in was still active. Result: page showed "Available" while the guest was physically in the room. Fix: stripped the room-flag write block from the command — it now only sets `t_booking.status = 0` for expired rows so the booking lists stop showing them, and never touches `m_rooms`. The check-out path (`CheckOutController::checkOut`) remains the sole writer that flips a room back to 0. **Backfills:** production room_id 31 → `rental_status = 1` (Ramdhan still in room); staging room_id 5 → `rental_status = 1` (occupant `UMH-260427339K1`). Pure command edit + targeted backfill — no migration / model / view / API change. |
| 2026-04-30 | Check-out — `rental_status` flip-to-0 guard (`CheckOutController::checkOut`) | The `$hasOtherOccupant` guard that decides whether to flip `m_rooms.rental_status` to 0 on checkout was matching ANY booking row with `status=1 AND check_in_at NOT NULL AND check_out_at IS NULL`, ignoring `transaction_status`. When a guest renewed and the renewal expired/got rejected/stayed pending, the parent's renewal-attempt rows kept `status=1` + the inherited `check_in_at` from the parent (set during the original physical check-in) + a NULL `check_out_at`, even though their transaction is `expired`/`pending`/`cancelled` — so the guard treated them as live occupants and refused to clear the flag, leaving the room marked Occupied with no actual guest. Added a `whereHas('transaction', $q->where('transaction_status','paid')->where('renewal_status',0))` clause so only paid, non-superseded transactions count as occupants. **Surfaced via**: KOST 2 #202 (room_id 55) — booking `UMH-260331557K2` checked out 2026-04-30 09:07:31, but renewal-attempt rows `1026` (expired) and `1027` (pending) — both with `check_in_at = 2026-02-19 16:17:08` and `check_out_at = NULL` — kept the guard truthy, leaving rental_status=1. **Backfill**: 3 rooms reset to `rental_status = 0` on production (room_id 5 KOST 1 #106, 16 KOST 1 #201, 55 KOST 2 #202). Rooms 5 and 16 are pre-existing pre-2026-04-24 stale flags that hadn't self-healed (no check-in/out had run on them since the semantics overhaul); room 55 is the bug above. Per the standing rule that `rental_status` is admin-controlled and never auto-flipped, this is a one-time targeted backfill, not a sweep. Future cycles self-heal naturally on the next check-in/out. Pure controller edit — no migration / model / view / API change. |
| 2026-04-29 | Room Types — auto-assign sort priority on create (`/properties/rooms/room-name-types`) | New room types created via the **+ Add Room Type** modal were inserted with `sort_priority = 0` (the DB default), so multiple rows piled up sharing priority 0 (1BR / 2BR / 3BR / Studio in the screenshot). This also broke the up/down reorder buttons against each other — `reorderRoomNameType()` uses strict `<` / `>` against `sort_priority`, so tied rows can't swap with siblings of the same value, only with the nearest non-tied neighbour. Fix: `ManajementRoomsController::storeRoomNameType()` now computes `(int) RoomNameType::max('sort_priority') + 1` and passes it to `RoomNameType::create()`, so each newly added type lands at the bottom of the list with a unique priority. Admins can still re-order afterwards using the existing chevrons. **Existing rows with sort_priority = 0** (4 rows on production per screenshot) need a one-time backfill — left as a follow-up (manual SQL or via the reorder UI once fresh values are assigned to one of the tied rows). Pure controller edit — no migration / model / view / API change. |
| 2026-04-29 | Check-Out page + renewal-rollback guard (`/bookings/checkout`) | Renewed bookings were re-appearing on the Check-Out page even after the guest had successfully renewed. Two compounding causes. **(1) Page query ignored `renewal_status`.** `CheckOutController::index()` and `filter()` only checked `t_transactions.transaction_status = 'paid'` + `check_out <= today` + `t_booking.check_out_at IS NULL`, so a parent transaction with `renewal_status = 1` (already superseded by a later paid order_id) still rendered as overdue. Added `where('renewal_status', 0)` to both query branches (default and `show_all_checkin`). **(2) Multi-attempt renewal rollback clobbered successful renewals.** When a renewal request is created, the parent booking's `check_out_at` is set to `now()` and the parent transaction's `renewal_status` is flipped to 1. If that renewal attempt later fails (expires / cancelled / payment rejected), the rollback in `Frontend API/.../Jobs/ExpireBooking.php`, `Frontend API/.../Api/BookingController::cancelBooking`, and Backend `PaymentController::reject()` / `cancel()` clears those parent fields back to NULL / 0. The rollback found "the parent" via `most-recent paid transaction older than the failing one`, but did **not** check whether a *newer* paid renewal of that same parent already exists — so a late-arriving expiry job (or a rejected/cancelled second-attempt renewal) wiped the state set by an earlier *successful* second-attempt renewal of the same parent, making the parent reappear on the Check-Out page. Added a `hasNewerSuccessfulRenewal` guard in all four rollback paths: `t_transactions WHERE room_id = ? AND user_id = ? AND idrec NOT IN (?, ?) AND is_renewal = 1 AND LOWER(transaction_status) IN ('paid','confirmed','completed') AND created_at > parent.created_at`. When true, the rollback is skipped (with a log entry) so the parent's `renewal_status` and `check_out_at` stay consistent with the surviving renewal. **Surfaced via**: Alvin Andriansyah / KOST 1 #210 (`UMH-260216402K1`) — first credit-card renewal `UMH-260315454K1` expired without payment at 16:14 on 2026-03-15, second attempt `UMH-260315888K1` was paid at 17:39, but the parent's `check_out_at` ended up NULL anyway (rollback ran late and clobbered the successful state). Carlos Tjhai's chain (`UMH-260220912K2`) shows the same shape. **Backfill**: corrected `t_booking.check_out_at` for `UMH-260216402K1` to `2026-03-15 17:39:39` (the moment the successful renewal was created). Carlos's `UMH-260220912K2` also needs `renewal_status = 1` + a `check_out_at` backfill — left for a follow-up sweep across all renewed-but-not-marked parents. |
| 2026-04-28 | Account Settings — hide Edit button for customers (`/settings/users-management/new`) | The new users-management page lists admins, site staff, **and** registered guests in the same table (controller `UserController::indexNew` returns all User rows, no role filter). Customers (`role_id IS NULL`, `user_type = 0`, `property_id = NULL`) showed an Edit pencil that opens an admin-form modal — wrong target audience. Wrapped the Edit button in `@if (!is_null($user->role_id))` so only admin/staff rows render it. Reset-Password (Superadmin-only) and the row itself remain visible. Pure Blade edit on `pages/settings/partials/users-table.blade.php`. |
| 2026-04-28 | Renewal — server-authoritative room_id (`Frontend API .../Api/BookingController::renewBooking`) | Companion fix to the room-transfer sync from 2026-04-27. The renewal endpoint built the new `t_transactions` row from the **client's** `request->room_id` (lines 1250–1251), but the new `t_booking` row from the **server's** active parent booking (line 1320). When the mobile app / web frontend held a stale `room_id` from before a room transfer (because the old `Transaction::room()` relationship read from `t_transactions.room_id`, which used to lag behind transfers), the renewal would write the OLD room into the new transaction while the new booking row got the correct room — so the next month's transaction was permanently mis-rooted. Surfaced via Allifian Welasabil (user 473): admin transferred her from Deluxe #306 → #202 on 2026-04-20, she renewed on 2026-04-27, and the new transaction (`UMH-260427922K1`) wrote `room_id = 36` (#306) while her new booking row correctly held `room_id = 17` (#202). **Fix:** added a server-authoritative resolver before `$transactionData` — looks up the parent order's currently-active `t_booking` (`Booking::where('order_id', $orderId)->where('status', '1')->first()`), and uses that `room_id` plus `Room::find(...)->name` for the new transaction, the new payment row, and the voucher-usage log. Falls back to the request payload only when no active parent booking exists. Also de-duplicated the second `$oldBooking = Booking::where(...)->first()` lookup (line 1328) to reuse the already-loaded `$activeParentBooking`. **Backfill:** one row corrected on production — `UMH-260427922K1` set to room_id 17, room_name "Deluxe". Sweep scoped to `is_renewal = 1` and excluded cancelled/expired/rejected/failed found no other orphans. |
| 2026-04-28 | Dashboard widget / Check-in & Check-out lists — guest contact fallback (`pages/bookings/checkin/partials/checkin_table.blade.php`) | Email and phone in the guest cell only read from `t_transactions.user_email` / `user_phone_number`. Some legacy booking flows leave those columns NULL even though the same data is correctly captured into `t_booking` and the `users` record — surfaced via `UMH-260317589K2` (Cathrien Florencia, paid 2026-03-17): widget showed `-` for phone while the booking row and user record both had `089636407617`. Added a fallback chain `transaction → booking → user → '-'` matching how the name field already falls back from `user.first_name + last_name` to `transaction.user_name`. Pure Blade edit — affects the dashboard "3 days before check-out" widget plus the Check-In and Checked-In booking pages that share this partial. No DB write or controller change. |
| 2026-04-27 | Room Transfer — sync transaction's room_id (`ChangeRoomController`) | Room transfers (`store()` and `rollback()`) only updated `t_booking` and `m_rooms` — `t_transactions.room_id` and `room_name` were left pointing at the original room forever. The Frontend "My Booking" page (`Frontend API/.../BookingController::index`) and the Mobile App resolve room via `Transaction::room()` (belongsTo Room on `t_transactions.room_id`), so transferred bookings kept showing the original room everywhere except the Backend admin views that read from `t_booking` directly. Surfaced via `UMH-260406754K1` — admin moved guest from Superior #112 to Superior #310 on 2026-04-08, but Frontend still showed #112. Added a `$currentBooking->transaction?->update(['room_id' => …, 'room_name' => …])` call after the booking deactivation in both `store()` (sync to new room) and `rollback()` (sync back to previous room). Backfilled 3 historical transfers in production where `t_transactions.room_id` was stale (`UMH-260406754K1`, `UMH-260330278K3`, `UMH-260310811K1`); the other 2 transfer orders were already in sync because they had been rolled back to the original room. |
| 2026-04-27 | Room Transfer History — pagination + guest email (`/rooms/change-room?tab=history`) | Two fixes. **(1) Pagination footer.** Production was still showing `Showing 1 of 5 (125 entries)` while only 3 rows actually rendered — the 2026-04-23 fix (switch candidate filter from `previous_booking_id IS NOT NULL` to `room_changed_at IS NOT NULL`) had been written locally but never deployed. Verified via `SELECT COUNT(DISTINCT order_id) FROM t_booking WHERE room_changed_at IS NOT NULL` = 5, matching what the page should show. Deployed `ChangeRoomController.php` to prod; counter and rendered rows are now in sync. **(2) Guest email under name.** Added `guest_email` to the items array (`firstBooking->user->email` with fallback to `t_booking.user_email`) and rendered a small grey `text-xs` line under the guest name in `transfer_history_table.blade.php` so admins can identify the guest at a glance without opening Detail. |
| 2026-04-27 | All Bookings — "Renewed at:" label for renewed parents (`/bookings/bookings`) | When a booking has been renewed, the parent's `t_booking.check_out_at` holds the timestamp the renewal was confirmed (not a real guest departure). Previously this rendered as "Check-out at: …" in the Check-Out column 3rd line, which misled admins into thinking the guest had checked out. Now switches the label to "Renewed at: …" (amber) when `transaction.renewal_status = 1` on the parent, and keeps the original "Check-out at: …" (indigo) for genuinely checked-out bookings. New i18n keys `allbookings_renewed_at` ("Renewed at:" / "Diperpanjang pada:") added to `Backend/lang/{en,id}/ui.php`. Note: the Status column badge for these renewed parents still shows "Check-out at: …" since `Booking::getStatusAttribute()` returns `Checked-Out` whenever `check_out_at` is non-null — left untouched here, can be revisited if the badge also needs the renewal-aware relabel. |
| 2026-04-27 | All Bookings — actual check-in/out datetime (`/bookings/bookings`) | Added a 3rd line to both the **Check-In** and **Check-Out** columns showing the actual admin-recorded datetime — `t_booking.check_in_at` (green) under the scheduled `transaction.check_in`, and `t_booking.check_out_at` (indigo) under the scheduled `transaction.check_out`. Format: `Check-in at: YYYY-MM-DD HH:mm` / `Check-out at: YYYY-MM-DD HH:mm`, reusing existing `ui.allbookings_checkin_at` / `ui.allbookings_checkout_at` translation keys. Lines only render when the booking actually has the corresponding timestamp set, so unbooked / not-yet-checked-out rows are unchanged. Mirrors the badge already shown in the Status column but surfaces the real datetime alongside the scheduled one for at-a-glance comparison. Pure Blade edit on `Backend/resources/views/pages/bookings/allbookings/partials/allbookings_table.blade.php`. |
| 2026-04-27 | Booking Payment — Verified badge column (`/payment/pay`) | Three tweaks to the Action column on the Booking Payment list. **(1) Payment method badge moved next to Verified.** Added a small purple `transaction_type` badge (e.g. `QRIS`, `VA`) to the **left** of the green Verified pill so admins can see the payment channel inline with the verification state instead of having to scan back to the Amount column. Renders on both the active-booking branch (paid/completed + no checkout) and the fallback branch. **(2) "By: DOKU" no longer shown when there is no admin verifier AND no DOKU verification.** Previously the fallback `verifiedBy->username ?? 'DOKU'` would print "By: DOKU" even when the row wasn't actually verified — looked like every payment was DOKU-confirmed. Now the verifier line only renders inside the Verified block, and "DOKU" only appears as the fallback when `verified_by` is null but the transaction is genuinely paid/completed (i.e. confirmed via the DOKU webhook, which doesn't write `verified_by`). When an admin manually approves, the username shows as before. **(3) Verified badge gated by real verification.** The fallback `@else` branch unconditionally rendered the Verified pill for any non-pending/non-waiting/non-paid-active state — including `rejected`, `failed`, `expired`, `canceled` rows, which are not verified. Added a `$isVerified = $payment->verified_at || transaction_status in [paid, completed]` guard so the Verified pill + "By:" line only render when the payment was actually verified. No controller / model changes; only `Backend/resources/views/pages/payment/pay/partials/pay_table.blade.php`. |
| 2026-04-27 | All Bookings — payment trio under check-in/out badge (`/bookings/bookings`) | Under the **Checked-In** and **Checked-Out** status badges, added a sub-block showing **Paid Method** (`t_transactions.payment_bank`), **Payment Confirm by** (admin who clicked confirm — pulled from `t_payment.verified_by` via the existing `payment.verifiedBy` eager-load), and **Payment date and time** (`t_transactions.paid_at`). Mirrors the existing Rejected/Canceled audit-trail pattern in the same partial. Falls back to `—` when any field is null (e.g., DOKU auto-confirmed bookings have no `verified_by`). New i18n keys `allbookings_paid_method`, `allbookings_payment_confirmed_by`, `allbookings_payment_at` added to both `Backend/lang/en/ui.php` and `Backend/lang/id/ui.php`. No controller / query changes needed — `payment.verifiedBy` was already being eager-loaded on `AllBookingController` for the Rejected sub-block. |
| 2026-04-24 | Room occupancy flag (`m_rooms.rental_status`) — new semantic | Redefined `m_rooms.rental_status` as a pure physical-occupancy flag: **1 = guest is in the room right now, 0 = room is empty**. Paid-but-future bookings no longer flip it. **Writes consolidated to two paths:** (a) `NewReservController::checkIn` sets it to 1 after the booking's `check_in_at` is written; (b) `CheckOutController::checkOut` sets it to 0 after the booking's `check_out_at` is written. Check-in adds a pre-guard: if `rental_status = 1` on the target room, return HTTP 409 with message "Room is currently occupied — please check out the current guest before checking in a new one." — prevents double-occupancy data bugs. Check-out adds a guard: only flip to 0 if NO other booking on the same room has `check_in_at IS NOT NULL AND check_out_at IS NULL AND status = 1` (covers same-day room-swap edge cases). **Stripped writes from every non-check-in/out path:** `Api/BookingController::store`, `BookingController::store` (non-API), `Api/BookingController::renewBooking`, `Api/BookingController::cancel` (both renewal-rollback and normal paths), `Api/DokuServiceController::recoverExpiredBookingState`, `Jobs/ExpireBooking::handle` (renewal rollback), Backend `PaymentController::reject`/`cancel` (both normal path + renewal rollback), `RefundController::refund`, and legacy `CheckInController::checkOut`. Kept: the manual admin toggle on the Room Availability page, and `ChangeRoomController` (room transfer is semantically a concurrent checkout + checkin so old → 0, new → 1). Rationale: cancellation is disallowed after check-in per business rule, so cancel/refund flows never need to touch the flag; booking/payment/renewal lifecycle events don't change physical presence. Existing DB data left as-is — it self-heals as check-ins / check-outs happen; admins can also manually correct via the Room Availability toggle. Affects both Backend Dashboard widget and Room Availability page, which continue to read `rental_status` as their single source of truth. |
| 2026-04-24 | Payment Proof modal dark mode (`/payment/pay`) | Header and footer of the Payment Proof modal rendered light (white/gradient header, `bg-gray-50` footer) in dark mode — title text and Close button were nearly invisible on both. The `.payment-proof-modal-header` / `.payment-proof-modal-footer` dark overrides already existed in `resources/css/app.css` (added 2026-04-16) but the compiled production bundle (`app-9dmhNq73.css`) contained zero occurrences of those rules — a stale build had been deployed since. Rebuilt with `npm run build` and deployed `public/build/` to both staging and production. New bundle `app-BPBprKak.css` includes all 11 `payment-proof-modal*` rules. **Also hit a deploy-pitfall**: initial upload only populated the repo's `public/build/` but Backend has a split-webroot layout — the actual served doc root (`~/public_html/admin.ulinmahoni.com/build/`) is separate from the repo and also needs the new manifest + hashed assets. Browser got 404 on the new CSS until both the manifest and the hashed file were also copied into the webroot's `build/` dir. Documented the split-webroot convention in CLAUDE.md under "Staging Deployment Notes". |
| 2026-04-24 | All Bookings — Rejected/Canceled audit trail (`/bookings/bookings`) | Under the Rejected and Canceled status badges, added a sub-block showing **when** the action happened and **which admin** did it (first + last name, falls back to username). **Rejected** pulls from `t_payment.verified_at` + `verifiedBy` (already populated by `PaymentController::reject()`), plus the existing reject note. **Canceled** pulls `cancel_at` from `t_transactions` and `requested_by` from the `t_refund` row created by `PaymentController::cancel()` — that column wasn't being populated before, so also patched `cancel()` to write `requested_by = Auth::id()` on refund creation. Eager-loaded `payment.verifiedBy` and `refund.requestedBy` on `AllBookingController` to avoid N+1 queries. |
| 2026-04-24 | All Bookings table (`/bookings/bookings`) | Two small admin-UX fixes on the bookings list. **(1) "Unknown" status badge on rejected bookings.** `Booking::getStatusAttribute()` switch had no branch for `transaction_status = 'rejected'` so the accessor fell through to `'Unknown'` (grey badge). Added `case 'rejected' → 'Rejected'` and registered a red `Rejected` badge in `statusClasses` / `statusLabels` in `allbookings_table.blade.php`. **(2) Booking creation timestamp under Booking ID.** Added a small grey `Y-m-d H:i` label under `$booking->order_id` in the same partial so admins can see when each booking was created without opening the detail page — useful for distinguishing renewals from originals at a glance. |
| 2026-04-24 | Payment Rejection (PaymentController) | Fixed renewal rollback on admin reject — when a renewal payment is rejected (`PaymentController::reject()`), the previous booking's `check_out_at` (set by the renewal flow) is now cleared, `renewal_status` is reset to 0, and `m_rooms.rental_status` is restored, matching the rollback already present in `cancel()` / Frontend `BookingController::cancel()`. Without this, the previous paid booking stayed "closed" after the rejected renewal, freeing the room on public listings even while the guest was still in their paid period. Also wrapped the method in a `DB::beginTransaction()` / `rollBack()` for atomicity. Surfaced via Kost 1 Room 317 — rejected renewal left the room showing "Tersedia" 5 days before the real check-out. |
| 2026-04-23 | Room Transfer History (`/rooms/change-room?tab=history`) | Pagination footer reported `Showing 1 of 5 (125 entries)` while only 3 rows actually rendered. `ChangeRoomController::getTransferHistory()` was selecting candidate orders by `previous_booking_id IS NOT NULL`, which also matches **renewals** (every renewal points its `previous_booking_id` at the prior period). The 125 candidates included 123 renewal-only orders that the per-row loop silently dropped via the `count() <= 1` guard, leaving the paginator's total/page-count out of sync with what got rendered. Switched the candidate query to `whereNotNull('b.room_changed_at')` (the column only set by `store()` / `rollback()` on real transfers) so the total now matches the rendered rows. Also extended the search filter to match `b.user_name`, `users.username`, `users.first_name`, and `users.last_name` — the input placeholder said "Order ID or Name" but only `order_id` was being searched. |
| 2026-04-22 | Payments table pagination (`/payment/pay`) | Pagination footer + entry count didn't refresh after search/filter — table swapped to filtered rows but footer kept showing `Showing 1 of 55 (438 entries)` (the unfiltered page count + grand total) and the page links still went up to page 55. The page's JS expected `data.pagination` from the AJAX response (index.blade.php line 412), but `PaymentController::filter()`'s JSON only returned `data.html` — `pagination` was never sent. Added the rendered pagination HTML to the JSON response, with `appends($request->except('page'))` so filter params are preserved when clicking page links. Parking + deposit filter endpoints already had this; only `/payment/pay` was missing. |
| 2026-04-21 | Add New Parking Payment modal (`/payment/parking`) | Inputs (text, select, textarea, datetime, file) blended into the modal body — white-on-white in light mode, and `dark:bg-gray-700` Tailwind v4 classes weren't compiled so the modal stayed white-bodied in dark mode too. Added a scoped `<style>` block that gives all inputs inside `#addPaymentModal` an explicit background distinct from the body in both modes (light: gray-50 inputs / white body, dark: gray-700 inputs / gray-800 body). Read-only fee field uses gray-100 (light) / gray-800 (dark) so it reads as disabled. |
| 2026-04-21 | Invoice Numbering — Parking format + Deposit approval | Two follow-ups to the invoice numbering rollout (2026-04-17) and the prior fix (2026-04-20). **(1) Parking now uses its own counter and a `PRK-` format prefix.** Previously `t_parking_fee_transaction` shared the booking counter (`invoice_code`), so booking and parking sequences were interleaved with no visual distinction (both rendered as `0001/K/KGA-INV/IV/2026`). Updated `Backend/.../InvoiceNumberService.php` so parking now uses counter key `PRK-{invoice_code}` (parallel to deposits' `DEP-{invoice_code}`) and the format segment `PRK-{property_initial}` — e.g. `0001/PRK-K/KGA-INV/IV/2026`. The three transaction types (booking / parking / deposit) now have fully independent per-property-per-year counters. **(2) Deposit invoices now assigned on admin approval.** `DepositPaymentController::approve()` only flipped `transaction_status` to `paid` without calling `InvoiceNumberService::assign()`, so deposits approved post-creation never received an invoice number. Added `InvoiceNumberService::assign($transaction->fresh())` after the status update — service is idempotent, so deposits already numbered via `store()` are unaffected. **(3) Backfill command updated.** `BackfillInvoiceNumbers` no longer merges bookings + parking into one chronological stream (they're independent counters now). Bookings walk the booking counter, parking walks the `PRK-{code}` counter, each ordered by `paid_at`. Added `--reset` flag (with confirmation prompt) that wipes post-cutoff `t_transactions.invoice_number` + `t_parking_fee_transaction.invoice_id` and deletes the matching `m_invoice_sequences` rows, then re-assigns cleanly — used to migrate existing post-2026-03-01 booking + parking numbers to the new gap-free + PRK-prefixed scheme. Deposits are intentionally untouched by `--reset`. Pre-cutoff (paid_at < 2026-03-01) numbers stay NULL. Frontend API mirror service docstring updated to reflect that the booking counter is no longer shared with parking. |
| 2026-04-20 | Invoice Generation — DOKU callbacks + Deposit | Closed two gaps in the persisted invoice numbering rollout (original rollout on 2026-04-17). **(1) DOKU-paid bookings stayed un-numbered.** `Frontend API/.../DokuServiceController.php` VA / QRIS / Credit Card callbacks only did `$transaction->update(['transaction_status' => 'paid', 'paid_at' => now()])` — they never called `InvoiceNumberService::assign()`, so every booking (with or without bundled parking) paid through the mobile/web checkout had `t_transactions.invoice_number = NULL`. Only admin-approved manual-proof payments were getting numbers live. Ported a minimal `InvoiceNumberService` to Frontend API (booking-only — no parking/deposit branch) and wired `::assign($transaction->fresh())` into all three DOKU callbacks immediately after the paid-state write. Service is idempotent (short-circuits on existing number) so retries are safe. **(2) Deposit invoice used hardcoded `KGA-INV` and a non-atomic sequence.** `Backend/.../DepositPaymentController::store()` generated `0001/DEP-XX/KGA-INV/I/2026` by (a) extracting initials from `property.name`, (b) hardcoding `KGA-INV` so every non-KGA property got a wrong invoice code, and (c) scanning `t_deposit_fee_transaction` with `preg_match + MAX(idrec)` which has a race under concurrent creates. Extended Backend's `InvoiceNumberService` with a `DepositFeeTransaction` branch that uses a distinct `m_invoice_sequences` counter key `DEP-{invoice_code}` (so deposit numbering is sequenced independently of booking/parking, starting from 1 per property/year), reads `property.initial` + `property.invoice_code` for the visible format `{seq:4}/DEP-{initial}/{invoice_code}-INV/{roman}/{year}`, and relies on the same `insertOrIgnore` + `lockForUpdate` atomic increment. Refactored `DepositPaymentController::store()` to drop the 50-line inline generator and call `InvoiceNumberService::assign($transaction)` instead. Service now resolves property via linked booking Transaction when deposit model lacks `property_id` directly. |
| 2026-04-20 | Dashboard — Occupancy counts & widgets | Fixed renewed bookings being double-counted across dashboard occupancy data. When a booking is renewed, the parent `t_booking.status` is flipped to 0 and the renewal becomes active — but four `DashboardController` queries were missing the `status = 1` filter, so the superseded parent was still counted as occupied. Symptoms: "Currently Occupied Rooms" widget showing the yellow `Check-Out Today` badge on already-renewed rooms (e.g. Kost 1 / 308); inflated 30-day occupancy chart; inflated avg revenue per occupied room; inflated Site-user header occupancy count. Added `->where('status', 1)` to `getOccupiedRoomsDetails()`, `getOccupancyHistory()`, `getRevenuePerOccupiedRoom()`, and the Site-user `currentOccupancy` query in `index()` — matching the existing filter already used on the `checkOuts` query. |
| 2026-04-16 | All Booking Pages | Fixed "latest booking per order" deduplication and status routing across all 6 booking pages. Added `scopeLatestPerOrder` to Booking model (uses `MAX(idrec) GROUP BY order_id` subquery) to ensure one row per transaction when multiple `t_booking` rows exist (room transfers, date changes). All Bookings: removed hard `t_booking.status=1` base filter, added `latestPerOrder()` + `transaction.status=1`; fixed dead "checked-out" dropdown filter (previously required status=1 which contradicts checkout setting status=0). Pending/Confirmed/CheckIn/CheckOut: added `latestPerOrder()` safety net, kept `status=1`. Completed: rewritten to require `t_booking.status=0` + `t_transactions.status=1` for checked-out bookings, `t_transactions.status=1` for cancelled. Transaction model `booking()` relationship now returns latest row deterministically (`orderByDesc('idrec')`), added `bookings()` hasMany. |
| 2026-04-16 | Payment Proof Modal (`/payment/pay`) | Fixed dark mode for payment proof popup modal — modal container, header, footer, and text were hardcoded light-mode only (white bg, blue-50 gradient header, gray-50 footer). Added `.payment-proof-modal` CSS class to all 3 modal instances (confirm payment, view proof, view proof read-only) with `html.dark` overrides in `app.css`: gray-800 body, dark blue header, gray-700 footer, light text colors. Light mode remains solid white (no glass). |
| 2026-04-10 | Booking Cancellation Modal (`/payment/pay`) | Fixed text alignment inside Cancel Booking modal — Warning banner heading and message now center-aligned; Booking Details card title and label/value rows now left-aligned; right-column form labels (Cancellation Reason, Refund Calculation, Refund Option, Refund Amount) now left-aligned. |
| 2026-04-09 | Booking Cancellation Modal (`/payment/pay`) | Fixed dark mode header color (was light pink/orange gradient, now solid dark red). Added new cancellation reason "Bukti pembayaran tidak sesuai dan pembayaran tidak ada"; selecting it locks Jumlah Pengembalian Dana to Rp 0 (read-only). |
| 2026-04-08 | Booking Cancellation (PaymentController) | Fixed renewal rollback on admin cancel — when a renewal booking is cancelled, the parent booking's `renewal_status` is now reset to 0 and its `check_out_at` is cleared, matching the Frontend API's existing rollback logic. |
| 2026-03-31 | Payments (`/payments`) | Internationalized all hardcoded strings in payment table — table headers, status badges, action buttons, cancel/reject/edit modals, notes editor, and empty state now use `__('ui.*')` translation keys for ID/EN support. |
| 2026-03-27 | Transaction Report (`/reports/payment`) | Fixed selected/hovered row background color in dark mode — was showing light gray-50 instead of subtle translucent highlight. |
| 2026-03-27 | Property's Rooms (`/m-rooms`) Edit Modal | Fixed modal centering — was trapped inside table by dark mode `backdrop-filter` on tbody. Added `no-backdrop-filter` to tbody. |
| 2026-03-27 | Property's Rooms (`/m-rooms`) Edit Modal | Fixed modal header color in dark mode — added dark blue background to match the blue/indigo gradient theme. |
| 2026-03-27 | Property's Rooms (`/m-rooms`) Edit Modal | Renamed "Price Type" to "Booking Type" in step 2. For daily type, hides price input and shows "Please use daily price management." message. Blocks booking type change if room has active/future bookings. |
| 2026-03-26 | Chat Dropdown (header widget) | Fixed dark mode transparency — dropdown was see-through due to parent backdrop-filter bleed. Added `isolation: isolate` and solid `background-color` for both chat list and chat window panels. |
| 2026-03-26 | Checked-In Users Modal | Fixed dark mode transparency — increased backdrop opacity (`bg-black/20` → `bg-black/50`, dark: `/70`), added `dark:bg-gray-800` to modal body, dark mode classes to search input, table header/body, and text elements. |
| 2026-03-26 | Add Property (`/properties` modal) | Initial field: increased `maxlength` from 3 to 10 characters (HTML + backend validation). |
| 2026-03-26 | Add Property (`/properties` modal) | Property Type buttons: added solid blue background + white text when selected for clear differentiation in dark mode. |
| 2026-03-26 | Add Property (`/properties` modal) | City dropdown: changed from `<select>` to text input with `<datalist>` — now auto-fills from map pin and accepts new city names. New cities auto-saved to `m_cities` master on property save. |
| 2026-03-26 | Add Property (`/properties` modal) | Facilities: selected items now use solid colored backgrounds (blue/green/purple) with white text instead of subtle tint that was invisible in dark mode. |
| 2026-03-26 | Chat (ChatController, ChatApiController) | Added FCM push notifications when admin sends chat messages to customers. |
| 2026-03-26 | Voucher Management (`/vouchers`) | Fixed pagination showing raw JSON when clicking page 2+. Filter route now redirects non-AJAX requests to index. Added JS handler to intercept pagination clicks and load via AJAX. |
| 2026-03-26 | Voucher Management (`/vouchers`) | Removed delete button from action column. Replaced status badge button with toggle switch (checkbox slider) matching Property master page pattern — blue when active, gray when inactive, with text label. |
| 2026-03-26 | Deposit Entry (`/payment/deposit`) | Fixed "Illegal mix of collations" error. `t_deposit_fee_transaction` used `utf8mb4_general_ci` while `t_transactions` used `utf8mb4_unicode_ci`. Added migration to convert table collation to `utf8mb4_unicode_ci`. |
| 2026-03-26 | Parking Entry (`/payment/parking`) | Fixed search/pagination error — filter route only accepted POST, pagination links used GET. Changed to `Route::match(['get','post'])`, added non-AJAX redirect to index, added JS pagination interceptor. |
| 2026-03-26 | Check-in Modal (Confirmed Bookings) | Fixed dark mode header — was nearly transparent (rgba 0.08). Now uses solid dark green background (`rgb(6,78,59)`) with white text for readability. |
| 2026-03-26 | Check-out Modal (Check-out Hari Ini) | Fixed dark mode header — was nearly transparent. Now uses solid dark amber background (`rgb(120,53,15)`) with white text for readability. |
| 2026-03-26 | Check-in Modal body (Confirmed Bookings) | Fixed dark mode body — was `bg-white` without dark class. Added `dark:bg-gray-800` to modal wrapper and `dark:bg-gray-700` to inner panels (guest profile, upload ID). |
| 2026-03-26 | All Modals (global CSS) | Fixed transparent modal bodies caused by global glass CSS rules (`.rounded-lg.border`, `.bg-white.rounded-lg`). Excluded `.shadow-xl` modals from glass rules and added solid white/gray-800 overrides. |
| 2026-03-26 | All Modals inner cards (global CSS) | Fixed inner content cards still too bright in dark mode. Added CSS overrides for `.shadow-xl` child elements: solid `gray-700` bg, `gray-600` borders, no backdrop-filter. |
| 2026-03-26 | Global dark mode (app.css) | Disabled glass CSS rules in dark mode entirely — restricted to `html:not(.dark)`. All `bg-white` elements now solid `gray-800`, `bg-gray-50` now solid `gray-900`. Fixes transparent body/cards on Change Room, modals, and all other pages. |
| 2026-03-26 | Check-in Modal Detail Pemesanan | Removed time from check-in/check-out. Added Total Booking, Deposit, Service Fee. Reorganized into 3-column layout: Booking (Order ID, Guest, Property, Room) · Stay (Check-in, Check-out, Duration) · Payment (Booking, Deposit, Service Fee, Total). |
| 2026-03-26 | Check-out Modal Detail Pemesanan | Added Total Booking, Deposit, Service Fee between Duration and Total Payment. Added `room_price`, `deposit_fee`, `service_fees` to checkout API response. Total Payment now bold green with separator line. |
| 2026-03-26 | Booking Cancellation (`/payment/pay`) | Allow cancellation with Rp 0 refund (no refund). Changed JS validation from `<= 0` to `< 0`. Added missing i18n key `enter_valid_refund_amount` in EN and ID. |
| 2026-03-26 | Daily Pricing Management (`/properties/calendar`) | Added activate/deactivate button to each entry in the "Entri Aktif" list. The `toggleEntryStatus()` JS method existed but had no UI button. Active entries show red "Nonaktifkan", inactive show green "Aktifkan". |
| 2026-03-26 | Room Type Management (`/properties/rooms/room-name-types`) | Fixed "route not found" error on store, update, and toggle-status. Fetch URLs were missing `/properties` prefix (used `/rooms/room-name-types/` instead of `/properties/rooms/room-name-types/`). |
| 2026-03-26 | All Bookings filter (`/bookings/bookings`) | Fixed filter search — `t_booking` used `latin1_swedish_ci` while `t_transactions` used `utf8mb4_general_ci`, causing collation mismatch on LEFT JOIN. Converted to `utf8mb4_general_ci`. Added non-AJAX redirect guard. |
| 2026-03-27 | All Bookings search (`/bookings/bookings`) | Fixed search returning 500 error — `order_id` and `property_id` columns were ambiguous after LEFT JOIN with `t_transactions`. Prefixed with `t_booking.` table name in both `index()` and `filter()` methods. |
| 2026-03-27 | Today's Check-Out search (`/bookings/checkout`) | Same ambiguous column fix — prefixed `order_id` and `property_id` with `t_booking.` in both `index()` and `filter()` methods. |
| 2026-03-27 | Deposit Entry (`/payment/deposit`) | Fixed "Illegal mix of collations" error — 24 tables used `utf8mb4_general_ci` while others used `utf8mb4_unicode_ci`, causing JOIN errors. Converted all tables to `utf8mb4_unicode_ci`. |
| 2026-03-27 | Language Switcher (global) | Fixed locale not persisting — `SetLocale` middleware now syncs DB locale to session as backup, and `updateLocale` also writes to session. Prevents revert to default `id` on transient auth failures. |
| 2026-03-27 | Deposit Entry pagination (`/payment/deposit`) | Fixed 405 "Method Not Allowed" on pagination — filter route only accepted POST but pagination links use GET. Changed to `Route::match(['get','post'])` and added non-AJAX redirect guard. |
| 2026-03-27 | Header (global) | Changed header to glassmorphism style — 35% opacity with 48px backdrop blur for both light and dark modes. Blur styles in inline `<style>` block because Vite/Lightning CSS strips unprefixed `backdrop-filter` needed by Firefox. |
| 2026-03-26 | Today's Check-Out filter (`/bookings/checkout`) | Same collation fix. Added non-AJAX redirect guard to filter method. |

---

## Frontend (Web Portal)

### New Modules

| Date | Page/Screen | Description |
|------|-------------|-------------|
| 2026-04-08 | All Pages | Web push notification support via Firebase Cloud Messaging — authenticated browser sessions now register FCM tokens and receive push notifications for all events (bookings, payments, etc.). |
| 2026-04-07 | Room Availability (API + Views) | Changed availability logic: daily rooms (`periode_daily=1`) are always shown as "Tersedia" (available). Monthly-only rooms compute availability dynamically by checking `t_booking` + `t_transactions` for active bookings instead of the static `rental_status` flag. New `is_available` field added to API responses alongside existing `rental_status` for backward compatibility. Affected: SearchController, BookingController, PropertyController, HomeController, HouseController, ApartController, RoomController, and all property/room blade templates. |
| 2026-04-07 | Property Detail (`/houses/{id}`) | Room category accordions now default to showing only available rooms. Added "Show all rooms" checkbox inside each category to reveal unavailable rooms. Uses Alpine.js `x-show` filtering. Translated in ID/EN/ZH. |

| Date | Page/Screen | Description |
|------|-------------|-------------|
| 2026-03-31 | My Bookings (`/bookings`) | User-initiated booking cancellation — "Batalkan" button on Upcoming tab (pending/waiting) and Completed tab (paid, not checked-in). SweetAlert2 modal shows refund breakdown preview, bank account form for QRIS/VA, and confirmation. |
| 2026-03-31 | Cancel API (`/api/v1/booking/{order_id}/cancel`) | New POST endpoint for cancelling bookings. Creates refund record, releases room and parking, sends FCM notifications. |
| 2026-03-31 | Cancel Preview API (`/api/v1/booking/{order_id}/cancel-preview`) | New GET endpoint returning refund breakdown calculation without cancelling. |
| 2026-03-31 | RefundCalculationService | Shared refund calculation service — tiered percentages for room+parking, 100% deposit, detects QRIS/VA for bank account requirement. |
| 2026-03-31 | Refund Model | New Eloquent model for `t_refund` table with user-initiated refund fields and relationships. |
| 2026-03-31 | FCM: `booking_cancelled` | New notification type added to push notification templates. |
| 2026-03-31 | Property & Room Description | Rich text HTML rendering for descriptions — `DescriptionHelper::getHtml()` sanitizes HTML (allows bold, italic, color, lists, links) while stripping dangerous tags. Backward compatible with plain text. Updated all 14 blade templates (property, room, house, villa, apartment, hotel). |
| 2026-03-30 | Property & Room Detail | Multi-language description display — shows description based on user's language selector (ID/EN/ZH). Fallback chain: selected locale -> EN -> ID. Added line break support via `nl2br`. |
| 2026-03-30 | Property & Room API | Added `description_parsed` / `descriptions_parsed` fields to API responses with per-language breakdown for mobile app. |
| 2026-03-27 | Home Page | Dynamic tagline loaded from database (random active tagline). Removed duplicate tagline text below hero heading. |
| 2026-03-27 | Home Page | Dynamic hero video — loads active video from Backend storage, falls back to bundled default video. |
| 2026-03-26 | Tickets Page (`/tickets`) | Full-page customer ticket management — split-pane layout with ticket list (status filter tabs) and chat panel. Supports text/image messages, close/reopen, 10s polling. |
| 2026-03-26 | Floating Ticket Widget | Bottom-right chat bubble on all public pages (authenticated users only). Expands to show ticket list with unread badges, links to `/tickets`. 30s polling. |
| 2026-03-26 | Ticket API (12 endpoints) | `/api/v1/tickets/*` — categories, eligibility, CRUD, messages, close/reopen, mark-as-read. `/api/v1/broadcasts/*` — list, detail. HEIC→JPEG conversion, FCM notifications. |
| 2026-03-26 | Ticket Models (8 models) | Ticket, TicketMessage, TicketAttachment, TicketRead, TicketCategory, TicketSequence, TicketBroadcast, TicketBroadcastRead + Role model. |
| 2026-03-26 | Ticket Service | Business logic for ticket creation, numbering (`{INITIAL}-TKT-{XXXX}`), eligibility checks (paid bookings + 7-day grace), close/reopen lifecycle, staff/HQ CS queries. |

### Bug Fixes

| Date | Page/Screen | Description |
|------|-------------|-------------|
| 2026-05-04 | My Bookings — sort by transaction creation date + show timestamp under guest contact (`/bookings`) | Two related changes. **(1) Sort order.** Bookings list was ordered by `check_in` desc, which buried freshly-created transactions (e.g. a just-paid renewal whose period starts next month) under historical bookings whose `check_in` happened to fall later. Switched the controller query in `BookingController::index()` from `orderBy('check_in', 'desc')` to `orderBy('created_at', 'desc')` so the most recently created transaction always lands at the top — matches how guests think about their booking history (most recent action first). All three tabs (Semua / Aktif & Mendatang / Selesai) inherit the new order via `$allBookings->filter()`, which preserves source-collection ordering. **(2) Created-at row in ID Pemesanan cell.** Added a 4th line under the order_id / guest name / phone stack showing `t_transactions.created_at` formatted as `d M Y H:i` (e.g. "11 Apr 2026 00:48") with a `far fa-clock` icon — gives guests/admins an at-a-glance way to distinguish renewals or repeated attempts on the same period. Applied uniformly to all three tab tables. Pure controller + Blade edit on `app/Http/Controllers/BookingController.php` and `resources/views/bookings/index.blade.php` — no migration / model / API change. |
| 2026-05-04 | My Bookings — show Bank + VA number for Virtual Account payments (`/bookings`) | The status column showed only the channel label (`VA - App`, `VA - Web`) for DOKU-issued Virtual Account payments — guests had no way to recall **which bank** they paid with or **what VA number** was issued without opening the original payment instructions. Added a third row beneath the existing payment-method badge that renders `{BANK} · {VA_NUMBER}` (e.g., `MANDIRI · 8618800000035134`) styled as a small monospace blue-tinted pill so it's visually distinct from the gray method badge above it. **Visibility rule** is gated by a new `isDokuVaPayment()` Blade helper that returns true only when both `t_transactions.virtual_account_no` and `payment_bank` are populated AND `transaction_type` is **not** in the excluded set (`BRI Manual`, `bri_manual`, `QRIS`, `qris`, `credit_card`, `CREDITCARD`, `Credit Card`, `promo`). The exclusion is important: BRI Manual stores a static fixed BRI account number in `virtual_account_no` (the account guests transfer to manually) — that's not a per-transaction DOKU VA, so showing it here would be misleading. **Channels that match**: the explicit `Transfer VA` (mobile-app DOKU VA) plus the bank-name aliases (`mandiri`, `bri`, `bsi`, `cimb`, `btn`, `danamon`) the web flow writes when DOKU returns the bank choice. Applied uniformly to all three tabs (Semua / Aktif & Mendatang / Selesai). Pure Blade edit on `bookings/index.blade.php` — no model / migration / controller / API change. The `virtual_account_no` and `payment_bank` columns already exist on `t_transactions` and are written by `Api/BookingController::generateVA` + the DOKU callbacks; reading them needs no model fillable change since fillable only governs mass-assignment. |
| 2026-04-23 | Room Detail (`/rooms/{id}`) — rental agreement consent | Updated the rental-agreement consent label on the room booking form. New text (Indonesian): "Saya menyatakan telah membaca, mengerti, memahami dan menyetujui :terms_link, :privacy_link, dan :rental_link". The three placeholders are substituted at render time with anchor tags pointing to `route('terms-of-services')`, `route('privacy-policy')`, and `route('rental-agreement')` — all open in new tab with `rel="noopener"`. Updated translations in `lang/{en,id,zh}/properties.php` (added `terms_link_text`, `privacy_link_text`, `rental_link_text` per language). Updated both `pages/room/show.blade.php` (i18n version) and `pages/room/en/show.blade.php` (was hardcoded English) to use the same translation pattern with link substitution. |
| 2026-04-23 | My Bookings (`/bookings`) — renewal modal & rules | Tightened the customer-side renewal flow on the web portal. **(1) Jumlah Bulan input.** Replaced the freeform `<input type="number">` (no max) with a `<select>` of 1-12 months. Options that would push the calculated `check_out` past `today + 15 months` are dynamically disabled by `refreshMonthsDropdown()` so the user can't even pick an out-of-range duration. **(2) 90-day window guard at button-click.** `openRenewModal()` now checks the chain's effective `previousCheckOut` upfront — if it's > 90 days away, shows the localised "renewal opens 90 days before check-out" SweetAlert and bails before opening the modal. If it's in the past, or the day-of-checkout but past 21:00, shows the "renewal window has closed" message instead. **(3) 15-month cumulative cap.** Both client-side (`submitRenewal`) and server-side (`renewBooking`) reject when the calculated new `check_out` would exceed `today + 15 months`. **(4) Server validator.** `booking_months` validation hardened from `nullable|integer|min:0` to `nullable|integer|min:0|max:12`. **(5) Server mirror of the window guard.** `renewBooking` runs the same 90-day + 21:00 check immediately after the re-anchor, returning a 422 with the localised message if violated — defence in depth against tampered clients. **(6) i18n.** Added 4 new keys to `lang/{en,id,zh}/booking.php` under the `js` namespace: `renewal_unavailable_title`, `renewal_window_too_early`, `renewal_window_closed`, `renewal_max_15_months`. Files touched: `Frontend API/.../resources/views/bookings/index.blade.php`, `Frontend API/.../app/Http/Controllers/Api/BookingController.php`, plus the three booking translation files. |
| 2026-04-17 | DOKU VA + CC payload (`Api/BookingController.php`) | Added `normalizeDokuPhone()` helper that strips every non-digit character (dashes, spaces, `+`, parens, dots, etc.) and rewrites the country prefix to DOKU's preferred `62XXXXXXXXXX` format. Handles all real-world variants in production (`+6282…`, `081-2345-6789`, `0812 3456 7890`, `+62085656598062` double-prefix, bare `81398718793`, etc.). Applied to both VA `virtualAccountPhone` (line 2535) and CC `customer.phone` (line 3084). Empty/null returns empty string so DOKU's optional-phone behavior still works. |
| 2026-04-17 | Payment expiry extended 15 → 30 min (server) | Bumped `t_transactions.expired_at` from 15 to 30 minutes in `BookingController.php:968` (web booking), `Api/BookingController.php:719` (API store), `Api/BookingController.php:1205` (API renewal). Aligned DOKU gateway-side expiry to match: VA `expiredDate` 60→30 min (`Api/BookingController.php:2493`), QRIS `validityPeriod` 1h→30 min (`Api/BookingController.php:2838`). All four layers (DB, gateway, web countdown, mobile countdown) now agree at 30 minutes — single source of truth. |
| 2026-04-17 | Payment QRIS countdown (`pages/payment/qris-show.blade.php`) | Fixed 5-minute cap on the QRIS countdown that contradicted the 15-min (now 30-min) server expiry — `maxMs = 5 * 60 * 1000` raised to `30 * 60 * 1000`. Users no longer see "Kedaluwarsa" while DOKU still accepts the payment. Also bumped fallback default in `pages/payment/show.blade.php:1646` from `'60M'` to `'30M'` for the case where DOKU response omits `validity_period`. |
| 2026-04-16 | DOKU Payment Callbacks (`DokuServiceController`) | Fixed race condition between ExpireBooking job and late DOKU payment callbacks. When a customer pays after the 15-minute expiry window (e.g. QRIS paid at 19 minutes), ExpireBooking had already rolled back booking/room/renewal state, but the DOKU callback then overrode `transaction_status` back to 'paid' without re-applying those updates — leaving `t_booking.status=0`, `m_rooms.rental_status=0`, and parent `renewal_status=0` in an inconsistent state. Added `recoverExpiredBookingState()` private method called from both VA and QRIS callbacks when `$wasExpired=true`: reactivates transaction+booking, re-occupies room, re-flags parent renewal_status, deactivates superseded parent bookings, restores voucher usage count. |
| 2026-04-15 | t_transactions (production data repair) | Repaired 16 currently-active monthly bookings whose `check_out` drifted from the chain-anchored expected end date. Traced each active booking back to its first check-in (same user + same room, continuous via prev.check_out = curr.check_in), summed `booking_months` across the chain, and recomputed the expected end as `head_check_in + total_months` with `min(head_day, daysInMonth)` clamping. 11 rows had +1 to +3 day overstay (most severe cluster: five day-31 chains from 2025-12-31 drifting +1/month); 4 rows were shortchanged by 1–2 days. One coordinated chain fix for user 833 / room 41 (`UMH-260317391K1` parent + `UMH-260415888K1` paid renewal) recovered a 29-day cascade overstay — parent check_out Mar 31→Apr 30, renewal dates shifted from May 1–Jun 30 to Apr 30–May 30. Rollback snapshot saved locally before execution. |
| 2026-04-15 | BookingController (`storeBooking` API) | Added defensive server-side monthly checkout clamp — `storeBooking` previously stored whatever `check_out` the client submitted, so legacy/old clients using naive `addMonths` (e.g. Mar 31 + 1 month → May 1 instead of Apr 30) produced rows with an extra day. The bad anchor then cascaded into renewal chains, producing up to a 29-day discrepancy when the next renewal combined the wrong check-in with `original_checkin_day=31`. Backend now ignores client `check_out` for monthly bookings and recomputes it from `check_in + booking_months` with `min(check_in_day, daysInMonth)` clamping, matching the existing `renewBooking` logic. |
| 2026-04-14 | Info Pages (Tentang Kami, Kerjasama, Korporasi, Sewa + EN) | Fixed missing footer icons on all 8 info pages (`/tentang`, `/kerjasama`, `/business`, `/sewa` and their English variants). Pages already included the shared `components.homepage.footer`, but the footer's Font Awesome icons (Apple/Play Store buttons, Login, WhatsApp, envelope, Instagram, TikTok, etc.) rendered as empty boxes because these pages never loaded the Font Awesome CDN — only the homepage templates did. Centralized the CDN link inside `components/homepage/styles.blade.php` so every page that already includes the homepage styles automatically receives Font Awesome, making the footer render identically across home, info, and listing pages. |
| 2026-04-14 | Listing Pages (`/rooms`, `/houses`, `/properties`) | Unified footer across all listing pages — Room, House, Property, and homepage-new index templates had hard-coded inline `<footer class="bg-gray-800">` blocks with static "Luxury Boulevard" placeholder content, while the home page used the full CMS-driven `components.homepage.footer` component (logo, app links, quick links, business, socials, payments). Replaced the 4 inline footers with `@include('components.homepage.footer')`. FooterComposer is already bound to the component view, so the included footer receives CMS data automatically on all 4 pages. |
| 2026-04-13 | Transaction Model (`app/Models/Transaction.php`) | Fixed monthly renewal end-of-month drift on production — `original_checkin_day` was missing from `$fillable`, so mass assignment silently dropped it on every `Transaction::create()`. Every renewal since 2026-04-01 was storing NULL, causing the mobile app to fall back to current check-in day and drift the billing day across chains (e.g. Jan 31 → Feb 28 → Mar 28 instead of Mar 31). Added `'original_checkin_day'` to `$fillable` and backfilled the 70 affected production rows using chain-ancestor lookup (same user+room, walks back to last known OCD, fallback to earliest chain check-in day). |
| 2026-04-10 | ExpireBooking Job | Fixed renewal rollback on booking expiry — when a renewal transaction expires due to unpaid timeout, the parent booking's `renewal_status` is now reset to 0 and `check_out_at` is cleared (and `m_rooms.rental_status` restored for monthly rooms), so the user can renew the parent booking again. Mirrors the existing cancel-flow rollback added in c309eba. |
| 2026-03-26 | User Model | Added `role()` relationship (belongsTo Role via `role_id`) for HQ CS user identification. |
| 2026-03-26 | BookingController (renew booking API) | Fixed `Class "App\Http\Controllers\Api\User" not found` error — added missing `use App\Models\User` import. Line 1258 used unqualified `User::find()`. |
| 2026-03-26 | Payment Page (`/payment/show`) | Fixed dark mode not applying — body had no background/text classes. Added `dark:bg-gray-900 dark:text-gray-100` to body, section, and headings. Removed empty `class=""` from `<html>` tag. |
| 2026-03-26 | Property Listing (gender badge) | Fixed gender badge (♂♀ label) hard to see in dark mode. Added semi-transparent white background, brighter text, and visible border. |
| 2026-03-27 | Header (homepage) | Changed `--glass-bg` from 18% to 35% opacity for light mode header, matching backend. |
| 2026-03-27 | Room Detail (daily booking) | Fixed check-out date picker becoming unresponsive after changing check-in date. Replaced `setOptions()` with destroy/recreate pattern for vanillajs-datepicker. Applied to both ID and EN versions. |

---

## Mobile App

### New Modules

| Date | Page/Screen | Description |
|------|-------------|-------------|
| 2026-03-31 | Booking Detail Page | User-initiated booking cancellation — "Batalkan Booking" red button on detail page for pending/waiting/paid (not checked-in) bookings. Opens cancel dialog with refund breakdown preview, bank account form for QRIS/VA, and confirmation. |
| 2026-03-31 | Cancel Booking Dialog | New dialog widget showing tiered refund calculation (room, deposit, parking), QRIS/VA bank account inputs, cancel confirmation. Refreshes booking list on success. |
| 2026-03-31 | Cancel Booking Data Layer | Repository (`cancel_booking_repository.dart`), model (`cancel_refund_model.dart`), provider (`cancel_booking_provider.dart`) — Riverpod 3.x Notifier pattern. |
| 2026-03-31 | Property & Room Description | Rich text HTML rendering for descriptions using `flutter_widget_from_html_core`. New `HtmlDescription` widget auto-detects HTML vs plain text content. Updated property detail and room detail pages. |
| 2026-03-30 | Room & Property Detail | Multi-language description support — displays description based on app locale (ID/EN/ZH) using `descriptions_parsed`/`description_parsed` from API. Fallback: current locale -> EN -> ID -> raw. |
| 2026-03-27 | Home Page | Dynamic tagline loaded from API (random active tagline), falls back to localized 3-part text. |
| 2026-03-27 | Home Page | Hero video caching — checks API for active video, downloads in background for next launch. Uses bundled asset as default fallback. |
| 2026-03-27 | Bottom Navigation Bar | Moved menu bar lower by reducing bottom margin from 12px to 0px. |
| 2026-03-26 | Ticket List Page (`/cs`) | Main customer service page with "My Tickets" and "Broadcasts" tabs. Shows ticket cards with status badges, unread counts, category labels. FAB for new ticket creation. |
| 2026-03-26 | Create Ticket Page (`/cs/create`) | Multi-step wizard: select type → select booking → select category → enter subject + message. Supports Suggestion Box (no booking required). |
| 2026-03-26 | Ticket Chat Page (`/cs/ticket/:id`) | Chat room for tickets — message bubbles, image send (camera/gallery), close/reopen actions, 5s polling. System messages for status changes. |
| 2026-03-26 | Broadcast Detail Page (`/cs/broadcast/:id`) | Read-only announcement view with title, message, sender info, metadata. Marks as read on open. |
| 2026-03-26 | Ticket Data Layer | 5 models (ticket, message, category, broadcast, eligible booking), repository (13 API methods), controller (StateNotifier), 6 Riverpod providers. |
| 2026-03-26 | API Constants | 12 new ticket/broadcast endpoint constants added. |

### Bug Fixes

| Date | Page/Screen | Description |
|------|-------------|-------------|
| 2026-04-17 | Payment countdown timers extended 15 → 30 min | Updated max-cap and default fallback across all payment dialogs and pages: `va_result_dialog.dart`, `qris_result_dialog.dart`, `paymentpage.dart` (4 cache spots: QR + CC, both new + switch flows), `mybookingdetails_page.dart` (transaction_date + 30 min computed expiry), and stale `(15 minutes from created_at)` comments in `payment_cache_utils.dart`. Source-only — takes effect on next mobile build. |
| 2026-03-26 | FCM Token Sync | Fixed endpoint mismatch: mobile was using `/api/users/fcm-token` but server expects `/api/v1/device-token`. Rewrote `FCMRepository` to use correct payload (`token`, `device_type`, `device_name`) with `DioClient` (auto-auth). |
| 2026-03-26 | FCM Service | Enabled token sync code (was commented out). Token now registers on login via `syncTokenToBackend()` and deletes on logout. |
| 2026-03-26 | Auth Provider | Added FCM token sync after successful login, FCM token cleanup on logout (was commented out). |
| 2026-03-27 | Ticket List Page (`/cs`) | Fixed "New Ticket" FAB hidden behind bottom nav bar. Added bottom margin (100px) to account for ShellRoute's BottomNavBar with `extendBody:true`. |
| 2026-03-27 | Register Page (signup) | Phone number input now follows frontend signup rules: strips leading zeros (matching frontend `replace(/^0+/, '')`), removed artificial "/15" max length counter, updated placeholder to "8123456789". |
| 2026-04-06 | Home Page | Fixed glass navbar transparency — disabled top SafeArea so content scrolls behind the navbar, enabling BackdropFilter blur effect to work. Added top padding in VideoSearchBanner so greeting text appears below navbar. |
| 2026-04-06 | Room Detail Page | Moved "Fasilitas Ruangan" section above "Biaya Tambahan" for better layout flow. |
| 2026-04-06 | Room Detail Page | Fixed daily pricing: subtotal now uses `multiTierTotalPrice` from price-preview API (per-date weekday/weekend/holiday rates) instead of flat rate × duration. |
| 2026-04-06 | Room Detail Page | Added daily pricing breakdown section under "Informasi Pemesanan" showing per-date prices with day type badges (weekday/weekend/holiday/seasonal). |
| 2026-04-06 | Payment Page | Replaced "Harga Harian" single line with per-day pricing breakdown table for daily bookings. Monthly bookings retain the original layout. |
| 2026-04-07 | Payment Page | Fixed DOKU gateway (VA/QRIS/CC) receiving flat rate × duration instead of multi-tier total. All payment method calls now use `_confirmedTotal` (set from `displayedTotal` at Bayar tap). |
| 2026-04-07 | Payment Page (renewal) | Fixed renewal DOKU amount — was using backend's flat `grandTotal`. Now uses `_renewalMultiTierTotalPrice` from price-preview API in `displayedTotal`. |
| 2026-04-07 | Renew Booking Dialog | Added per-day price breakdown (DailyPriceBreakdown widget) and subtotal for daily bookings. Breakdown is fetched from price-preview API when dates change. |
| 2026-04-07 | Cancel Booking Dialog | Fixed "gagal membatalkan booking" false error after successful cancel — autoDispose provider was disposed during async API call (no listener). Fixed by watching provider in build(). Added AppLogger tracing throughout cancel flow. |
| 2026-03-29 | Dependencies | Ran `flutter pub upgrade` — updated 50 packages (minor/patch). Key updates: dio 5.9.2, camera 0.11.4, shared_preferences 2.5.5, video_player 2.11.1, flutter_svg 2.2.4, google_sign_in_ios 6.3.0, logger 2.7.0. |
| 2026-03-29 | Payment Providers | Migrated `DokuCCNotifier`, `DokuQRISNotifier`, `DokuVANotifier`, `PaymentNotifier`, `VoucherNotifier` from `StateNotifier`/`StateNotifierProvider` to Riverpod 3.x `Notifier`/`NotifierProvider`. |
| 2026-03-29 | Room Details Providers | Migrated `RoomDetailsNotifier`, `AvailabilityCheckNotifier` from `StateNotifier`/`StateNotifierProvider` to Riverpod 3.x `Notifier`/`NotifierProvider`. |
| 2026-03-29 | Room Filter (Detail Property) | Migrated `selectedRoomFilterProvider` and `selectedRoomStatusFilterProvider` from `StateProvider.family` to Riverpod 3.x family `Notifier`/`NotifierProvider.family`. |
| 2026-03-29 | Room Details Page, Video Banner | Replaced deprecated `.valueOrNull` with `.value` (nullable by default in Riverpod 3.x). |
| 2026-03-29 | Room Detail, Property Detail | Multi-language descriptions — room and property models now parse `descriptions_parsed` / `description_parsed` from API. Detail pages resolve description by app locale (`id`, `en`, `zh`) with fallback chain. |
